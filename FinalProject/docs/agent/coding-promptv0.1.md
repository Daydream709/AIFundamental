你是资深深度学习时间序列预测工程师，请严格按照以下完整需求，为我生成Project2时序预测任务**全套高性能可直接运行代码**，专门适配【Linux环境 + RTX 4090 24G显存】，基于PyTorch框架，支持最大batch、高效率训练，完整复现SOTA模型+创新模块，代码无BUG、可直接训练、可直接写进实验报告。

========================
【一、核心项目信息】

1. 任务：多变量长时序预测 + 三模态融合（时序数值 + 文本模态 + 时序转图像模态）
2. 预测范式：历史序列H → 未来序列F
3. 运行环境：Linux + CUDA 12.x + PyTorch 2.x + RTX 4090 24G 显存
4. 数据集：ETTm2、Weather、Electricity、EPA-Air（4个标准数据集）
5. # 目标：全SOTA模型训练 + 创新模型 + 消融实验 + 可视化 + 结果表格

【二、必须实现的模型清单】
=== 基线模型 ===

1. DLinear
2. PatchTST
3. iTransformer

=== 2025 最新SOTA模型 === 4. TimeMixer++（ICLR2025）5. LightGTS

=== 时序大模型零样本预测 === 6. Chronos2（零样本推理，不微调）

=== 创新自研模型（必须完整实现）=== 7. 改进iTransformer：频域注意力 + 周期分解 + 自适应归一化 8. 三模态融合预测模型：时序特征 + 文本Embedding + 时序转图像视觉特征 + 跨模态门控融合 9. 概率预测模型：高斯NLL损失，输出预测值+置信区间

========================
【三、4090 24G 专属高性能配置（必须严格遵守）】

1. 设备：cuda:0（单卡4090）
2. 混合精度训练：启用 torch.cuda.amp 自动混合精度
3. 梯度累计：按需开启，最大化利用显存
4. 数据加载：num_workers=8，pin_memory=True
5. 随机种子：42（完全可复现）
6. 允许最大Batch、最大序列长度，不爆显存
7. 优化数据读取速度，适配Linux高性能训练

========================
【四、统一实验超参数（固定不可修改）】
历史序列长度 H = 96
预测长度 F = 96 / 192
批次大小 BatchSize = 64~128（适配4090 24G）
训练轮次 Epochs = 100
早停耐心值 = 10
优化器 = AdamW
学习率 = 1e-4
权重衰减 = 1e-5
损失函数：MSE、MAE、高斯NLL
评价指标：MSE、MAE、RMSE

========================
【五、必须实现的完整功能模块】

1. 自动数据集下载、加载、预处理
   - 缺失值填充、标准化、时序滑窗构建
   - 多模态特征生成
2. 文本模态：使用预训练模型生成文本特征
3. 图像模态：将1D时序转为2D recurrence plot灰度图
4. 统一训练/测试/推理框架
5. 模型参数量、推理速度、显存占用统计
6. 消融实验自动化运行
7. 高清图表可视化（预测曲线、指标对比、效率对比）
8. 自动保存实验结果表格（CSV格式）
9. 最优模型自动保存

========================
【六、消融实验列表（必须全部实现）】

1. 单时序模态 vs 时序+文本 vs 三模态融合
2. 原始iTransformer vs 加入频域注意力
3. 原始模型 vs 加入周期分解
4. 点预测(MSE) vs 概率预测(NLL)
5. SOTA模型 vs 创新模型

========================
【七、代码结构要求】
按Linux训练规范组织代码，单文件完整可运行：

1. 环境依赖安装（Linux版）
2. 导入库 + 随机种子设置
3. 配置类（Config）
4. 数据集加载与多模态构造
5. 所有模型定义（标准SOTA + 创新模型）
6. 训练、验证、测试函数
7. 指标计算、模型效率统计
8. 可视化函数
9. 主函数（一键启动所有实验）

========================
【八、交付要求】

1. 代码可在Linux 4090 24G环境直接运行，无需修改路径与参数
2. 开启高效训练，不爆显存
3. 模型严格遵循SOTA论文结构
4. 创新模块完整可训练
5. 自动输出所有图表、结果表格、最优模型
6. 注释详细，可直接用于课程报告
